# TASK

**모델**: `(O)` Opus · `(S)` Sonnet · `(H)` Haiku
**상태**: `[ ]` 예정 · `[~]` 진행중 · `[x]` 완료

---

## 토스증권 API — 보유 해외주식 표시

> 로컬은 실데이터로 동작 중(`TOSS_MOCK=0`). 배포만 IP 문제로 남음.

- [ ] (O) **[TASK-2] Vercel 배포** — 서버리스 IP 비고정 → 허용목록과 충돌. 고정 IP 프록시 또는 고정 IP 서버(VPS) 필요. (해결 전까지 배포본은 `TOSS_MOCK=1`로 목업 표시)

---

## 완료

- [x] (S) **[TASK-1] 실데이터 연결** — 헤더 `X-Tossinvest-Account`=accountSeq, 응답 `result.items[]`, rate×100. `TOSS_MOCK=0`, 로컬 실데이터 확인 완료
- [x] (S) **[TASK-3] 플로우 탭 분리** — 홈에서 종목발굴/실적점검/포폴점검 탭 전환
- [x] (O) **[TASK-4] 포폴 점검 분기별 분리** — 포폴 탭에 1~4분기 카드 + 권장 시기 표기
- [x] (S) **[TASK-5] 보고서 탭 통합** — "보고서" 탭 추가, 종목별 모아보기 + `+ 새 종목 발굴` 버튼(시작점 모달)
- [x] (S) **[TASK-6] 탭 재배치** — 보고서 탭을 맨 앞으로, 종목발굴 탭 제거(발굴은 보고서 탭 `+`로 진입). 기본 탭=보고서
- [x] (S) **[TASK-7] 포폴 탭 레이아웃 버그** — 원인=스크롤바 유무에 따른 가운데정렬 시프트. `html { scrollbar-gutter: stable }`로 해결
- [x] (S) **[TASK-8] 키컬러 팔레트** — `#6A39C0` = `brand-500` 스케일(50~900) 정의, 모든 플로우/탭/버튼 brand로 통일
- [x] (S) **[TASK-9] 탭 전환 애니메이션** — `key={flowTab}` + `.tab-panel` fade/slide-up (reduced-motion 대응)
- [x] (H) **[TASK-10] `+` 버튼 간소화** — 보고서 탭 버튼 `+`만(아이콘형), aria-label 유지
- [x] (H) **[TASK-11] 새로고침 아이콘화** — 보유 배너 새로고침 SVG 아이콘(로딩 시 회전)
- [x] (S) **[TASK-12] 헤더 로고화** — 소제목 제거 + `現` 그라디언트 로고마크 + 그라디언트 타이틀
- [x] (S) **[정리] ReportsView 제거** — "전체 보기" 삭제, 보고서 열람 보고서 탭으로 일원화
