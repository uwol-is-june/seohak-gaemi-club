# TASK

**모델**: `(O)` Opus · `(S)` Sonnet · `(H)` Haiku
**상태**: `[ ]` 예정 · `[~]` 진행중 · `[x]` 완료
**번호**: 각 태스크에 `[TASK-N]` 부여 (지칭용, 완료돼도 번호 재사용 안 함)

---

> **운영 방식: 로컬 전용** (`npm run dev`). 배포 안 함 — 토스 IP 허용목록에
> 실행 PC의 공인 IP만 등록하면 됨. 네트워크/자리 바뀌면 IP 재등록 필요.

---

## 예정

_(없음)_

---

## 완료

> TASK-92~95는 2026-07-31 코드리뷰(미커밋 작업분 30개 파일, +1163/−1079)에서 나온 항목이다.
> 리뷰 방식: 5개 병렬 에이전트(CLAUDE.md 준수 / 버그 스캔 / git 히스토리 / 과거 지적 재발 / 주석 계약)
> → 발견 항목별 검증 에이전트로 신뢰도 채점 → 80점 미만 제외. 괄호 안 점수가 검증 신뢰도다.

- [x] **[TASK-95]** (S) 트랙레코드 '사유' 컬럼 제거 — **의도된 축소로 확인** (조치 없음)
  - [dashboard/components/TrackRecordView.tsx](dashboard/components/TrackRecordView.tsx)에서 '사유' 컬럼이
    빠졌고 `reason` 필드([dashboard/lib/calls.ts:34](dashboard/lib/calls.ts#L34))는 API·데이터 모델로만 흐른다
  - 채점 로직(`scoreCall`)은 무변경 — **채점 정확성 영향 없음**. 사용자가 의도한 축소임을 확인해 종결
  - 원장 원본(`data/calls.jsonl`)에는 그대로 남으므로 생존편향 방지 취지 자체는 유지된다

- [x] **[TASK-94]** (H) 토큰 예산 작업의 문서 정합성 마감 3건
  - **범위 표기 불일치 해소**: `skills/industry-funnel.md`를 5~10M → **5~12M**으로 고쳐
    [CLAUDE.md:93](CLAUDE.md#L93) 표와 일치시킴. 전체 스킬 7개 범위 표기가 CLAUDE.md 표와 일치함을 검증
  - **docstring↔구현 불일치 해소**: `run_batch` docstring이 "실패 개수를 종료코드로"라 했으나 구현은
    0/1 반환 → **docstring을 구현에 맞춤**(실패 개수는 요약 줄에 출력). 종료코드 계약을 실측 검증
    (실패 2건 → exit 1, 전부 성공 → exit 0)
  - **잘못된 규칙 참조 수정**: `report_audit.py --max` help의 `TB-6` → **`TB-3`**(조사 상한)

- [x] **[TASK-93]** (H) 실적 캘린더 하단 에러 배너가 다른 탭의 낡은 에러를 표시 (85점)
  - 가드는 축을 검사하는데 출력 `{callsError ?? error}`에 **축 게이팅이 빠져** 있었다.
    `callsError`는 축 전환 시 초기화되지 않아('calls'가 아니면 로드 effect가 조기 반환)
    '트랙레코드 전체'에서 실패 → '보유 종목'에서도 실패 시 **낡은 메시지**가 떴다
  - 수정: `bannerError = axis === "calls" ? callsError ?? error : error` 상수를 도입해
    같은 파일 `emptyText`(176-183줄)와 **동일한 축 게이팅 규칙**으로 통일. 배너는 이 상수만 참조

- [x] **[TASK-92]** (H) `sync_sector_map.py` 섹터 충돌 경고가 도달 불가능한 죽은 코드 (85점)
  - 바깥 가드 `cand[:2] > cur[:2]`와 안쪽 검사 `cur[:2] == cand[:2]`가 **상호배타적**이라
    경고 분기에 절대 진입하지 못했다 → 같은 티어·같은 날짜 섹터 충돌이 **경고 없이** 조용히 결정됨
  - 수정: 우선순위 승리 케이스를 `return`으로 먼저 빼고, **동률+섹터 상이**를 별도 분기로 분리.
    먼저 스캔된 값을 유지하고(파일 순회가 `sorted()`라 결정적) 경고를 남긴다.
    격리 재현으로 6개 경로 검증(종목 마커 우선 / 최신 날짜 우선 / 동률 동일섹터 무경고 / 동률 상이섹터 경고)
  - **수정 즉시 그동안 숨어 있던 실제 충돌이 드러남**: CEG가 `AI-Infrastructure`(5개 파일 + 퍼널
    보고서)와 `Nuclear-Power`(`04-RiskManagement-LiLu-Perspective.md` 1개)로 갈려 있었다.
    CLAUDE.md 규칙(퍼널 파일명의 섹터 토큰과 정확히 일치)대로 이상치를 `AI-Infrastructure`로 정정 →
    경고 0건, 26종목/9섹터 정상 배정 확인

- [x] **[TASK-91]** (O) 종목 보고서 유형에 '실적 점검' 탭 분리 — 심층분석 하위에서 독립
  - `{티커}-earnings-{기간}[-거장].md` 를 `deep-dive` → 신규 `earnings` 카테고리로 이동
  - 유형 탭 순서: 열등주 스크리닝 · 버핏 6-게이트 · 심층분석 · **투자 논제 수립 · 실적 점검** · 급변동 분석
  - 3차 생성일자 탭: 같은 분기 파생본(`-버핏`·`-독자검토` 등)을 `2026Q2 · 버핏` 으로 구분,
    접미사 없는 확정본을 앞에 정렬(유형 탭 기본 선택이 파생본으로 안 잡히게)

- [x] **[TASK-90]** (O) 티커 → 섹터 자동 배정 — 보고서 섹터 마커 → 대시보드 분야·섹터 위계
  - 퍼널 마커 `<!-- funnel sector: X | finalists: A, B, C -->` + 종목 마커 `<!-- meta sector: X -->`
  - `tools/sync_sector_map.py` 가 스캔 → `app_config('sector_auto_map')` upsert (Stop 훅에서 발행 직후 실행)
  - 대시보드는 `mergeAutoSectorGroups()` 로 **수동 우선, 빈 곳만 자동** 병합. 그룹 편집 모달은
    자동 배정분을 '미분류'와 분리해 표시(저장 값에는 미포함)
  - 기존 퍼널 보고서 8건 마커 백필 → 24종목 자동 배정

---

_(마지막 사용 번호: TASK-95, 다음은 TASK-96부터)_
