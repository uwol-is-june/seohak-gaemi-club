logs/command-log.jsonl
# 병목 신호 일일 스캔 로그 (로컬 실행 기록 — 보고서는 Supabase로 발행됨)
logs/bottleneck-scan/
reports/美团/command-log.jsonl
.DS_Store
reports/.DS_Store

# 雪球爬虫登录态缓存（含 cookies，本地使用）
/tmp/xueqiu_state.json

# Claude Code 개인 설정 (로컬 전용 hook 등)
.claude/settings.local.json

# Claude Code 에이전트 메모리 (로컬 전용)
memory/

# Python 캐시
__pycache__/
*.pyc

# Node 의존성 (카드 렌더러·대시보드)
node_modules/
tools/cards/package-lock.json

# 카드뉴스 산출물 — tools/cards/{slug}.json 에서 바이트 단위로 재생성된다.
# 원본은 JSON이므로 이미지를 커밋하면 발행할 때마다 저장소만 커진다.
#   재생성: node tools/cards/render.mjs tools/cards/{slug}.json
assets/cards/

# 환경변수 (키/시크릿 포함 — 절대 커밋 금지)
.env.local
.env*.local
